exports.handleLogout = (request, response) => {
    // Your logout logic here. For example, clearing user session or tokens.
    // This is a placeholder implementation.
    response.status(200).send({ message: 'Logout successful' });
};